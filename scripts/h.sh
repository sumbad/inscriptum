# TODO: scripts for:

# CLI
    # "h:console": "hasura console --admin-secret myadminsecretkey",
    # "h:migrate": "hasura migrate apply",
    # "h:migrate-create": "hasura migrate create --from-server --admin-secret myadminsecretkey",
    # "h:metadata": "hasura metadata export --admin-secret myadminsecretkey",
    # "h:update-cli": "hasura update-cli"

# Publish
    # git remote -v
    # git push heroku master


# Squash migrations and add checkpoints to version control
    # hasura migrate squash --name "changes_to_use_page" --from 1609172197076 --log-level DEBUG --admin-secret myadminsecretkey